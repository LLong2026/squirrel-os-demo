# The SQUIRL OS Demo — What To Watch For

Five minutes. That's all this needs.

## The 5-Minute Mind-Blow Script

1. **The dashboard (0:00)** — health score 100, 0 false positives, 25 nodes, 10 agents. Every number on screen is real and computed from live entity data — nothing is canned.
2. **Inject an anomaly (0:30)** — the demo ships with chaos injection: DNS cache poisoning, gas regression, latency spikes, cosmic ray bit flips.
3. **Watch it heal (1:00)** — Detect → Isolate → Heal, in seconds. The playbook executes, the anomaly resolves, an audit trail records every step.
4. **Watch it LEARN (2:00)** — the healing event updates the Pattern entity and LearningMetrics. The next occurrence of the same anomaly heals faster.
5. **The deterministic cage (3:00)** — ask the system to do something it shouldn't deploy. It refuses. The mesh proposes — the runtime validates. Zero-touch deployment is disabled; a human approves.
6. **Three tenants (4:00)** — personal, business, sandbox, side by side. Try to leak data across. You won't.

## The Dual Panel

Every Runtime Era surface shows both panels: the **6-layer neural mesh** (Ingest, Feature, Classify, Match, Heal, Learn) and the **deterministic runtime pipeline** (Semantic Creation → Envelope Generation → Validation → Healing Loop), each stage producing a signed artifact.

Caption: *"The mesh proposes — the runtime validates."*

## Demo data honesty

All seeded demo/simulation data is labeled as simulated. All metrics in the demo are computed live from the actual ledger — averages labeled as simulated are labeled as simulated. The system never lies about its numbers, and that's the point.

---

**Contact Support:** support@squirlos-technologies.com · squirlos-technologies.com · **8 patents pending + 5 SBIR tracks**

> **Disclaimer:** This software is a prototype and is provided for educational and research purposes only. It is not intended for production use, commercial deployment, or safety-critical environments. All systems are experimental and may contain defects.
